# H-19 補完: フォーク元（pacexy/flow）の固定値を取り除き、CI/CD 側で
# `SENTRY_ORG` / `SENTRY_PROJECT` / `SENTRY_AUTH_TOKEN` を環境変数で渡す運用にする。
defaults.url=https://sentry.io/
# defaults.org=YOUR_ORG     # 環境変数 SENTRY_ORG で上書き
# defaults.project=YOUR_PRJ # 環境変数 SENTRY_PROJECT で上書き
